name = "jusik-scheduler"
main = "worker.js"
compatibility_date = "2026-07-01"

# 5분마다 깨어나서, 그 시각에 돌려야 할 워크플로우를 worker.js 의 SCHEDULE 표로 판단한다.
# (크론 트리거를 하나만 쓰므로 무료 플랜 한도를 신경 쓸 필요 없음)
[triggers]
crons = ["*/5 * * * *"]
